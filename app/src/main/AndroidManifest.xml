<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finalproject">
        <activity
            android:name=".finishfile"
            android:exported="false" />
        <!-- Launcher Activity -->
        <activity
            android:name=".getstarted"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Create account (email) -->
        <activity android:name=".createaccount_email" /> <!-- Create account (phone) -->
        <activity android:name=".createaccount_phone" /> <!-- Sign in (email) -->
        <activity android:name=".signin_email" /> <!-- Dashboard -->
        <activity android:name=".signin_phone" />
        <activity android:name=".dashboard" />
        <activity android:name=".travel_safety" />
        <activity android:name=".personal_safety" />
        <activity android:name=".gotosettings" />
        <activity android:name=".emergency_type" />
        <activity android:name=".add_emergency_contact" />
        <activity android:name=".alerthistory" />
        <activity android:name=".finishpage" />
        <activity android:name=".otp" />
        <activity android:name=".emergency_contacts" />
    </application>

</manifest>